import React from 'react';

const Button = () => {
	return (
		<button className='btn btn-primary btn-block grid grid-cols-1 place-items-center hover:bg-blue-600 text-white rounded-full'>
			Continue
		</button>
	);
};

export default Button;
